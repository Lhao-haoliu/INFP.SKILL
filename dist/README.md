# dist

This directory is for local distilled outputs and drafts.

Large or machine-generated intermediate files are ignored by default. Only manually reviewed, safe, abstract Markdown drafts should be considered for commit.
